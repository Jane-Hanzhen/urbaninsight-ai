# AI Decision Agent Specification

## Overview

The AI Decision Agent interprets authoritative results produced by the Analysis Engine.

It supports initial analysis, follow-up conversation, borough comparison and Markdown report generation.

The AI layer is provider-agnostic.

The frontend uses the same endpoints in Mock AI and Live AI modes.

The AI must never calculate or modify PCA, TOPSIS, scores, contributions or rankings.

---

# Architecture

```text
FastAPI AI Endpoint
↓
Context Builder
↓
Prompt Builder
↓
Provider Factory
↓
Mock Provider or Live Provider Strategy
↓
Validated AnalysisInsights or TextResponse
```

Source structure

```text
backend/app/ai/
├── agent.py
├── context.py
├── prompt_builder.py
├── schemas.py
└── providers/
    ├── base.py
    ├── factory.py
    ├── mock_provider.py
    ├── openai_provider.py
    ├── qwen_provider.py
    └── deepseek_provider.py
```

---

# AI Responsibility

The AI Agent may:

- Explain stored rankings
- Interpret stored indicators
- Identify main drivers
- Summarize strengths
- Summarize weaknesses
- Generate recommendations
- Compare stored borough results
- Answer contextual questions
- Generate reports

The AI Agent must not:

- Calculate PCA
- Calculate TOPSIS
- Derive new weights
- Generate a new ranking
- Change stored values
- Invent missing values
- Claim causality that the data does not support

---

# Provider Strategy Pattern

All providers implement one interface.

Required provider capabilities

- Return provider name
- Return configured model
- Report whether configuration is complete
- Generate structured `AnalysisInsights`
- Generate normal text

The abstract interface is:

```text
AIProvider
├── name
├── model
├── is_configured()
├── generate_insights(prompt)
└── generate_text(prompt)
```

Live providers inherit from `OpenAICompatibleProvider`.

They use the OpenAI Python SDK and non-streaming Chat Completions.

---

# Supported Providers

## OpenAI

Provider value

```text
openai
```

Configuration

- `OPENAI_API_KEY`
- `OPENAI_MODEL`

The default OpenAI API base URL is used.

Default model

```text
gpt-4o-mini
```

## Qwen

Provider value

```text
qwen
```

Configuration

- `DASHSCOPE_API_KEY`
- `QWEN_MODEL`
- `QWEN_BASE_URL`

Default model

```text
qwen3.7-plus
```

Default base URL

```text
https://dashscope.aliyuncs.com/compatible-mode/v1
```

Qwen requests include:

```json
{
  "enable_thinking": false
}
```

Thinking mode and streaming are disabled.

## DeepSeek

Provider value

```text
deepseek
```

Configuration

- `DEEPSEEK_API_KEY`
- `DEEPSEEK_MODEL`
- `DEEPSEEK_BASE_URL`

Default model

```text
deepseek-v4-flash
```

Default base URL

```text
https://api.deepseek.com
```

---

# AI Modes

## Mock Mode

Configuration

```text
AI_MODE=mock
```

Behavior

- Never initializes an external LLM client
- Never uses an API key
- Ignores `AI_PROVIDER`
- Reports provider `mock`
- Reports model `urbaninsight-mock`
- Returns predefined structured insights
- Returns predefined chat, comparison and report text
- Supports English and Simplified Chinese
- Simulates 300–800ms latency

Mock mode is the required mode for routine UI development.

## Live Mode

Configuration

```text
AI_MODE=live
```

Behavior

- Resolves `AI_PROVIDER`
- Creates the selected provider strategy
- Requires the active provider API key for generation
- Uses the configured provider model and base URL
- Makes external API calls

If `AI_MODE` is omitted, the backend defaults to `live`.

If `AI_PROVIDER` is omitted in live mode, the backend defaults to `openai`.

Unsupported modes or providers raise configuration errors.

---

# Context Builder

The Context Builder is:

```text
backend/app/ai/context.py
```

Input

- Borough ID

Required database records

- Borough
- Indicators
- Analysis result

Output

```json
{
  "borough": {
    "id": "E09000007",
    "name": "Camden",
    "region": "London"
  },
  "scores": {
    "overall": 0,
    "regional_rank": 0,
    "economic": 0,
    "social": 0,
    "ecological": 0
  },
  "indicators": {},
  "analysis_engine": {
    "method": "PCA-weighted TOPSIS",
    "dimension_contributions": {},
    "indicator_contributions": {},
    "pca_weights": {},
    "pca": {},
    "topsis": {
      "overall_score": 0,
      "regional_rank": 0
    }
  }
}
```

The Context Builder excludes database timestamps from the indicator payload.

If required data is missing, the AI endpoint returns a not-found response.

---

# Prompt Builder

The Prompt Builder is:

```text
backend/app/ai/prompt_builder.py
```

System rules

- Use only supplied Analysis Engine context
- Treat statistical values as immutable
- Never calculate or estimate missing results
- Distinguish evidence from recommendations
- Avoid unsupported causal claims
- Keep decision support concise and practical

Every prompt includes:

- Selected borough context
- Optional comparison borough context
- Up to 12 prior conversation messages
- Explicit output-language instruction

Prompt types

- Initial analysis prompt
- Follow-up chat prompt
- Comparison prompt
- Markdown report prompt

The context is serialized as formatted, sorted JSON.

Raw prompt text must not be sent to the frontend or written to normal logs.

---

# Structured AnalysisInsights

`POST /ai/analyze` accepts `include_ai_insights`.

When false, it uses the existing preset provider to return complete deterministic
`AnalysisInsights` without an external API call. When true, it uses the request
`ai_provider` to create a live DeepSeek or Qwen provider. If omitted, `AI_PROVIDER`
is the server default. `AI_MODE=mock` overrides live provider creation for every
endpoint, including an explicit web analysis request, and returns predefined local
content without an external call.

Provider configuration or request failure returns the same complete preset insights
as a basic fallback, with metadata showing that live AI was requested but not applied.

Response envelope

```json
{
  "analysis_mode": "ai",
  "ai_insights_requested": true,
  "ai_insights_applied": true,
  "ai_provider": "deepseek",
  "ai_model": "configured model",
  "ai_error": null,
  "insights": {}
}
```

`analysis_mode` is `basic` unless live insights were successfully validated.
`insights` is always present for a successful analysis response.

Schema

```json
{
  "executive_summary": "string",
  "ranking_explanation": "string",
  "main_drivers": [
    {
      "title": "string",
      "detail": "string"
    }
  ],
  "strengths": [],
  "weaknesses": [],
  "indicator_interpretation": "string",
  "recommendations": [
    {
      "title": "string",
      "detail": "string",
      "priority": "High"
    }
  ]
}
```

Cardinality

- `main_drivers` → 2–4 items
- `strengths` → 2–4 items
- `weaknesses` → 2–4 items
- `recommendations` → 2–4 items

Recommendation priority

- `High`
- `Medium`

Live providers are instructed to return one JSON object only.

The request asks for JSON object output.

Provider content is parsed with `json.loads` and validated with Pydantic.

JSON code fences are tolerated and removed.

Invalid JSON or schema failure becomes a sanitized provider response error.

---

# Generation Contracts

Initial analysis, chat, and comparison request structured JSON and validate it with
Pydantic. Chat and comparison responses also retain a compatibility summary:

```json
{
  "content": "string"
}
```

Plain text generation remains available for legacy report requests. The current
Markdown export instead renders deterministically from the completed analysis
snapshot.

All generation is non-streaming.

Empty assistant content is treated as a provider response error.

---

# Structured Follow-up Conversation

Endpoint

```text
POST /ai/chat
```

Input

- Selected borough ID
- User question
- Previous context
- Optional comparison borough ID
- Locale

Question length

```text
1–1000 characters
```

Message length

```text
1–4000 characters
```

Previous context is limited to 12 messages.

The current frontend uses the dedicated comparison endpoint for comparison controls.

Response contract

```text
ChatResponse
├── content                compatibility summary
└── answer: ChatAnswer
    ├── response_type      insight | recommendation | clarification
    ├── headline
    ├── summary
    ├── key_points[]       maximum 4; positive | neutral | attention
    ├── bottom_line?
    └── limitations?
```

The structured answer is schema-validated for both Mock and Live providers. The
frontend renders it as a card; `content` remains for compatibility and conversation
context.

---

# Borough Comparison

Endpoint

```text
POST /ai/compare
```

The endpoint builds complete context for:

- Selected borough
- Comparison borough

The AI explains:

- Supplied score differences
- Indicator differences
- Contribution differences
- Competitive advantages
- Development gaps

It must not derive a new score or ranking.

Response contract

```text
CompareResponse
├── content                compatibility summary
└── answer: CompareAnswer
    ├── response_type      comparison
    ├── headline
    ├── summary
    ├── primary_advantages[]
    ├── comparison_advantages[]
    ├── primary_positioning
    ├── comparison_positioning
    ├── decision_note
    └── evidence[]         maximum 3
```

## Dynamic Mock Responses

In `AI_MODE=mock`, chat and comparison use the same database-backed context as Live
mode but never create an external client. The Mock response builder recognizes
supported intents including overview, strengths, weaknesses, ranking and development
direction. Comparisons calculate display differences only from supplied stored
scores and indicators; they do not rerun PCA or TOPSIS.

## Thinking and Retry

Generation remains non-streaming. The frontend displays a thinking status while a
chat or comparison request is pending. This is interface feedback, not model
chain-of-thought. If a request fails, the frontend stores its type and payload so the
user can retry it without losing messages or rerunning the selected borough analysis.

## Conversation PDF Export

Endpoint

```text
POST /conversations/pdf
```

The request contains the selected borough, locale, and one to 100 ordered messages.
Assistant messages may include their structured `ChatAnswer` or `CompareAnswer`.
ReportLab renders the existing conversation into a bilingual PDF. Export performs no
provider request and no statistical recalculation.

---

# Report Generation

Markdown endpoint

```text
POST /ai/report
```

The endpoint returns Markdown text.

Required sections

- Executive Summary
- Regional Evaluation
- Key Indicators
- Strengths
- Weaknesses
- Recommendations

The report must begin with a level-one title.

The current frontend sends the completed-analysis snapshot:

- Completed analysis metadata
- Completed structured `AnalysisInsights`
- The borough analysis-result snapshot
- Locale and report mode

When that snapshot is present, the backend renders Markdown deterministically from
the supplied completed insights and authoritative SQLite context. It does not call
OpenAI, DeepSeek, Qwen or the Mock Provider, and it does not recalculate PCA/TOPSIS.

For backwards compatibility and future extension, `POST /ai/report` still accepts
the legacy request containing `include_ai_insights` and no completed snapshot.
Only that legacy path retains the previous provider-generated report behavior.

Titles

- English basic: `UrbanInsight Basic Analysis Report`
- English AI: `UrbanInsight AI In-depth Analysis Report`
- Chinese basic: `UrbanInsight 基础分析报告`
- Chinese AI: `UrbanInsight AI 深度分析报告`

The frontend converts the response into a Markdown download. The active UI always
uses the completed-snapshot path, regardless of Mock or Live analysis mode.

PDF endpoint

```text
POST /reports/pdf
```

PDF generation is deterministic document rendering, not an AI operation. The
frontend supplies the completed `AnalysisInsights` and completed-analysis metadata;
the backend reloads the authoritative borough indicators and stored Analysis Engine
result from SQLite. It then renders a bilingual A4 report with ReportLab.

The PDF route must not call `generate_insights`, `generate_text`, or any external
provider. The report title, provider/model disclosure and fallback note are derived
from `ai_insights_applied` and the other frozen completion metadata, not from the
current header controls.

PDF is the primary user-facing export. Markdown remains available as a secondary,
editable format.

---

# Language Support

Supported locale values

- `en`
- `zh-CN`

English is the default when `locale` is omitted.

Unsupported locale values fail request validation.

The requested locale applies to:

- Initial analysis
- Follow-up chat
- Borough comparison
- Report generation

Keep these values unchanged:

- Official borough names
- Database IDs
- Indicator IDs
- JSON field names
- Numerical values
- `High` and `Medium` schema values

Changing the frontend language affects new requests only.

Existing responses remain in their original language.

---

# AI Status

Endpoint

```text
GET /ai/status
```

Mock example

```json
{
  "configured": true,
  "provider": "mock",
  "model": "urbaninsight-mock"
}
```

Live example

```json
{
  "configured": true,
  "provider": "deepseek",
  "model": "deepseek-v4-flash"
}
```

Status reflects the current environment.

It does not expose API keys or base URLs.

---

# Error Handling

Configuration errors

- Missing active provider key → generation fails with service unavailable
- Unsupported mode → service unavailable
- Unsupported provider → service unavailable

Provider API errors

- Log provider name
- Log exception type
- Log HTTP status when available
- Log request ID when available
- Log sanitized error and cause
- Return generic `502` detail

Provider response errors

- Log provider name
- Log exception type
- Log sanitized message
- Return generic `502` detail

Sanitization removes:

- Configured provider keys
- Strings matching common `sk-...` key patterns

The frontend displays localized generic errors.

---

# Security

Never:

- Commit API keys
- Put keys in frontend environment variables
- Return keys from status endpoints
- Log raw API keys
- Expose raw provider exceptions to the frontend
- Send prompts to browser logs

Secrets belong only in:

```text
backend/.env
```

---

# Testing

Automated tests cover:

- OpenAI configuration
- Qwen configuration
- DeepSeek configuration
- Provider switching
- Mock mode
- Missing keys
- Unsupported mode and provider
- Structured parsing
- Structured chat and comparison parsing
- Dynamic Mock intent and borough-context behavior
- Conversation PDF export without provider calls
- Thinking-state and retry state orchestration
- Invalid JSON
- Normal text generation
- Qwen thinking disabled
- Error redaction
- AI status
- English fallback
- Simplified Chinese prompts and responses
- Invalid locale validation

External provider calls are mocked in tests.

Automated tests must not make paid LLM calls.
